{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Tests: coverage (.env.local via make)",
      "type": "shell",
      "command": "make",
      "args": [
        "test-all"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": "test",
      "problemMatcher": []
    }
  ]
}
