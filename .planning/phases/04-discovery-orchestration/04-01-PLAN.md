---
phase: 04
plan: 01
name: discovery-orchestration
issue: 20
requirements: [DISC-01, DISC-02, DISC-03, DISC-04]
depends_on: [01,02,03]
autonomous: true
---
# Phase 04 Plan — Discovery orchestration

<task type="auto">
<name>04.1 Define source adapter and capability contracts</name>
<files>src/github_stars_contrib_mcp/domain/ports/source_adapter.py, tests/unit/domain/test_source_adapter_contract.py</files>
<action>Define SourceAdapter with `supports(source)`, `capabilities(source)`, and async `iter_items(source,cursor)` semantics. Capability reports available/limited/unavailable plus credential/permission reason. Source adapters emit SourceItem+Evidence only.</action>
<verify>pytest -q tests/unit/domain/test_source_adapter_contract.py</verify>
<done>Fake adapters express paging/cursor/capability without importing Stars clients.</done>
</task>

<task type="auto">
<name>04.2 Normalize source items into candidates</name>
<files>src/github_stars_contrib_mcp/application/discovery/normalizer.py, tests/unit/application/test_candidate_normalizer.py</files>
<action>Map provider-neutral fields to proposed Stars title/description/url/date/type. Prefer deterministic structured metadata. Unsupported/ambiguous type becomes review-required with reason instead of guessed publication data. Record normalizer version in provenance.</action>
<verify>pytest -q tests/unit/application/test_candidate_normalizer.py</verify>
<done>Fixtures produce stable candidates and ambiguous inputs remain reviewable rather than fabricated.</done>
</task>

<task type="auto">
<name>04.3 Implement discovery run orchestrator</name>
<files>src/github_stars_contrib_mcp/application/discovery/orchestrator.py, tests/unit/application/test_discovery_orchestrator.py</files>
<action>For enabled sources: resolve adapter, load cursor, iterate items, normalize, atomically persist candidate+evidence, update cursor after successful item/page, and record run summary. Isolate failures per source; classify auth/rate-limit/parse/security errors. Support dry_run that persists only run diagnostics, not candidates/cursors.</action>
<verify>pytest -q tests/unit/application/test_discovery_orchestrator.py</verify>
<done>Fake multi-adapter run proves idempotency, failure isolation and cursor semantics.</done>
</task>

<task type="auto">
<name>04.4 Wire adapter registry without real providers</name>
<files>src/github_stars_contrib_mcp/di/discovery.py, src/github_stars_contrib_mcp/shared.py, tests/unit/di/test_discovery_wiring.py</files>
<action>Add a small composition module that builds repository, fetcher, adapter registry and orchestrator. Keep server tool registration unchanged until Phase 11.</action>
<verify>pytest -q tests/unit/di/test_discovery_wiring.py</verify>
<done>Composition can run a fake adapter end-to-end in an offline test.</done>
</task>
