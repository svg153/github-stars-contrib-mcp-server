---
phase: 02
plan: 01
name: identity-and-source-registry
issue: 18
requirements: [IDENT-01, IDENT-02, IDENT-03, IDENT-04]
depends_on: [01]
autonomous: true
---
# Phase 02 Plan — Identity bootstrap and source registry

<task type="auto">
<name>02.1 Canonicalize source URLs and classify provider type</name>
<files>src/github_stars_contrib_mcp/application/discovery/source_identity.py, tests/unit/application/test_source_identity.py</files>
<action>Implement deterministic normalization: lowercase scheme/host, remove default ports/fragments, normalize trailing slash conservatively, preserve meaningful query params unless provider rule explicitly removes tracking params. Classify github, youtube, rss, website, sessionize, pretalx, x, linkedin or unknown. Do not fetch URLs.</action>
<verify>pytest -q tests/unit/application/test_source_identity.py</verify>
<done>Equivalent URLs map to one source key and provider-specific identity tests pass.</done>
</task>

<task type="auto">
<name>02.2 Bootstrap sources from existing Stars/profile data</name>
<files>src/github_stars_contrib_mcp/application/use_cases/bootstrap_sources.py, tests/unit/application/test_bootstrap_sources.py</files>
<action>Consume existing get-user/profile/link/contribution use cases or ports. Explicit profile links become ownership=explicit. Domains repeatedly present in prior contribution URLs may become ownership=inferred with evidence count, never verified. No arbitrary outbound crawl occurs.</action>
<verify>pytest -q tests/unit/application/test_bootstrap_sources.py</verify>
<done>Bootstrap is idempotent and records why each source exists; inferred sources are visibly lower trust.</done>
</task>

<task type="auto">
<name>02.3 Add source registry management use cases</name>
<files>src/github_stars_contrib_mcp/application/use_cases/manage_sources.py, tests/unit/application/test_manage_sources.py</files>
<action>Add list/add/verify/reject/disable source operations. Explicit user verification can promote inferred→verified; system inference cannot. Disabling preserves history/cursors but blocks sync. Rejecting records reason and prevents immediate re-bootstrap unless evidence changes.</action>
<verify>pytest -q tests/unit/application/test_manage_sources.py</verify>
<done>State transition tests enforce trust rules and source registration is idempotent.</done>
</task>

<task type="auto">
<name>02.4 Wire persistence only</name>
<files>src/github_stars_contrib_mcp/shared.py, tests/unit/test_shared.py</files>
<action>Create the discovery repository from settings during application bootstrap using lazy/explicit construction matching existing shared.py patterns. Do not register new MCP tools yet.</action>
<verify>pytest -q tests/unit/test_shared.py</verify>
<done>Existing server bootstrap remains compatible and discovery storage can be instantiated in tests.</done>
</task>
