---
phase: 07
plan: 01
name: youtube-adapter
issue: 23
requirements: [YT-01, YT-02, YT-03]
depends_on: [04]
autonomous: true
---
# Phase 07 Plan — YouTube discovery adapter

<task type="auto">
<name>07.1 Normalize trusted YouTube channel identity</name>
<files>src/github_stars_contrib_mcp/application/discovery/youtube_identity.py, tests/unit/application/test_youtube_identity.py</files>
<action>Normalize channel ID/handle/custom URLs but store canonical channel ID when known. Do not infer ownership from a single video URL. Explicit profile link is explicit ownership; API-confirmed channel mapping preserves that evidence.</action>
<verify>pytest -q tests/unit/application/test_youtube_identity.py</verify>
<done>Equivalent channel URLs resolve to one registry identity in fixtures.</done>
</task>

<task type="auto">
<name>07.2 Implement Data API adapter with quota-aware capability</name>
<files>src/github_stars_contrib_mcp/infrastructure/adapters/youtube_source.py, tests/unit/infrastructure/test_youtube_source.py</files>
<action>Use YouTube Data API v3 when `YOUTUBE_API_KEY` is configured: resolve channel/uploads playlist, page videos, fetch metadata in batches and emit stable video IDs/canonical URLs. Classify quota/auth errors explicitly. Do not scrape HTML.</action>
<verify>pytest -q tests/unit/infrastructure/test_youtube_source.py</verify>
<done>Mocked API pagination/batching/quota tests pass with stable SourceItems.</done>
</task>

<task type="auto">
<name>07.3 Add public channel-feed limited fallback</name>
<files>src/github_stars_contrib_mcp/infrastructure/adapters/youtube_feed_source.py, tests/unit/infrastructure/test_youtube_feed_source.py</files>
<action>When a canonical channel ID is known and API key absent, use the public YouTube channel Atom feed through safe fetch for recent uploads only. Capability must say limited and describe reduced history/metadata.</action>
<verify>pytest -q tests/unit/infrastructure/test_youtube_feed_source.py</verify>
<done>Fallback works offline with fixtures and never pretends full historical coverage.</done>
</task>

<task type="auto">
<name>07.4 Register adapters/settings and contract test</name>
<files>src/github_stars_contrib_mcp/config/settings.py, src/github_stars_contrib_mcp/di/discovery.py, env.template, tests/contract/test_youtube_discovery_contract.py</files>
<action>Add optional API key, register full/limited adapter selection and verify source→candidate persistence with canonical evidence.</action>
<verify>pytest -q tests/contract/test_youtube_discovery_contract.py</verify>
<done>Both available and limited paths produce truthful capability/evidence behavior.</done>
</task>
