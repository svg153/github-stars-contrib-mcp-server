---
phase: 10
plan: 01
name: dedupe-confidence
issue: JIT
requirements: [DEDUPE-01, DEDUPE-02, DEDUPE-03, DEDUPE-04]
depends_on: [05,06,07,08,09]
autonomous: true
---
# Phase 10 Plan — Deduplication, confidence and conflicts

<task type="auto">
<name>10.1 Implement deterministic fingerprints</name>
<files>src/github_stars_contrib_mcp/application/discovery/fingerprint.py, tests/unit/application/test_fingerprint.py</files>
<action>Canonicalize URL first; include provider external ID where available; normalize Unicode/title whitespace/date/type. Produce exact-source, exact-url and normalized-content fingerprints separately so reasons remain inspectable. Never hash descriptions as the only identity.</action>
<verify>pytest -q tests/unit/application/test_fingerprint.py</verify>
<done>Equivalent URLs/titles produce expected stable fingerprints and materially distinct entries do not collapse.</done>
</task>

<task type="auto">
<name>10.2 Build duplicate matcher against Stars and queue</name>
<files>src/github_stars_contrib_mcp/application/discovery/deduplicator.py, tests/unit/application/test_deduplicator.py</files>
<action>Load current Stars contributions through existing read use case and local candidates. Rank exact provider ID, exact canonical URL, then normalized title/date/type window. Record match target, method and reason. Do not mutate/merge candidates automatically.</action>
<verify>pytest -q tests/unit/application/test_deduplicator.py</verify>
<done>Exact duplicates are blocked, likely duplicates are conflicts and unrelated items remain clear.</done>
</task>

<task type="auto">
<name>10.3 Implement explainable confidence scoring</name>
<files>src/github_stars_contrib_mcp/application/discovery/confidence.py, tests/unit/application/test_confidence.py</files>
<action>Separate ownership confidence from contribution confidence. Use deterministic weighted signals: verified source, canonical ID, structured date/title/type, speaker/author identity, corroborating evidence and ambiguity penalties. Return score band plus individual reasons; no opaque model-only score.</action>
<verify>pytest -q tests/unit/application/test_confidence.py</verify>
<done>Golden fixtures produce stable scores/reasons and low-evidence social/event findings stay review-heavy.</done>
</task>

<task type="auto">
<name>10.4 Integrate dedupe/confidence into orchestration</name>
<files>src/github_stars_contrib_mcp/application/discovery/orchestrator.py, tests/contract/test_discovery_dedupe_contract.py</files>
<action>After normalization, compute fingerprints, duplicate match and confidence before state becomes review_ready. Exact existing-Stars duplicates become blocked_duplicate; likely matches remain conflict/reviewable. Persist all reasons/provenance.</action>
<verify>pytest -q tests/contract/test_discovery_dedupe_contract.py</verify>
<done>Replay and cross-provider fixtures cannot create publishable duplicates silently.</done>
</task>
