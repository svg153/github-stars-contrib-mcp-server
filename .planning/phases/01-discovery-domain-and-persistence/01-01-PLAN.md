---
phase: 01
plan: 01
name: discovery-domain-and-persistence
issue: 17
requirements: [DOMAIN-01, DOMAIN-02, DOMAIN-03, DOMAIN-04]
depends_on: []
autonomous: true
---
# Phase 01 Plan — Discovery domain and persistence

## Goal
Create stable local discovery state before any source-specific implementation.

<task type="auto">
<name>01.1 Define versioned discovery entities and lifecycle</name>
<files>src/github_stars_contrib_mcp/domain/discovery.py, tests/unit/domain/test_discovery.py</files>
<action>Create enums/value objects for source type/ownership, candidate state, duplicate state and review decision; dataclasses/Pydantic models for SourceRecord, SourceItem, Evidence, Provenance, CandidateContribution and DiscoveryRun. Candidate states: discovered → review_ready → approved|rejected|deferred → published; edited approved candidates remain approved only when policy-relevant fields are revalidated. Add schema_version=1. No HTTP/MCP imports.</action>
<verify>pytest -q tests/unit/domain/test_discovery.py</verify>
<done>Serialization round-trips; required fields validate; invalid lifecycle transitions raise a typed domain error.</done>
</task>

<task type="auto">
<name>01.2 Define repository ports</name>
<files>src/github_stars_contrib_mcp/domain/ports/discovery_repository.py, tests/unit/domain/test_discovery_repository_contract.py</files>
<action>Define protocols for SourceRepository, CandidateRepository, DiscoveryRunRepository and UnitOfWork-like transaction boundary. Required methods include get/list/upsert source, save/list/get candidate, record review/publication metadata, save/get cursor and begin/finish run. Keep signatures provider-neutral.</action>
<verify>pytest -q tests/unit/domain/test_discovery_repository_contract.py</verify>
<done>Fake in-memory implementations satisfy contract tests without importing infrastructure.</done>
</task>

<task type="auto">
<name>01.3 Implement SQLite adapter and schema bootstrap</name>
<files>src/github_stars_contrib_mcp/infrastructure/persistence/discovery_sqlite.py, tests/unit/infrastructure/test_discovery_sqlite.py</files>
<action>Use stdlib sqlite3; create schema_version table plus sources, candidates, evidence, runs, cursors and reviews/publication metadata. Enable foreign keys. Transactions must make candidate+evidence writes atomic. JSON fields use deterministic UTF-8 serialization. Database path comes from constructor, not globals.</action>
<verify>pytest -q tests/unit/infrastructure/test_discovery_sqlite.py</verify>
<done>Fresh bootstrap, reopen, rollback and round-trip tests pass using tmp_path; no new dependency is required.</done>
</task>

<task type="auto">
<name>01.4 Add discovery DB setting without wiring provider logic</name>
<files>src/github_stars_contrib_mcp/config/settings.py, env.template, tests/unit/test_settings.py</files>
<action>Add `DISCOVERY_DB_PATH` with a local default under the user's cache/data location or an existing repository convention. Do not create directories at import time. Document it in env.template.</action>
<verify>pytest -q tests/unit/test_settings.py && python -m compileall -q src</verify>
<done>Settings load with default and override; no network/provider code added.</done>
</task>
