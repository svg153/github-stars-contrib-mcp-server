---
phase: 05
plan: 01
name: rss-and-website-adapters
issue: 21
requirements: [RSS-01, RSS-02, RSS-03]
depends_on: [04]
autonomous: true
---
# Phase 05 Plan — RSS/Atom and trusted websites

<task type="auto">
<name>05.1 Implement feed parser without network coupling</name>
<files>src/github_stars_contrib_mcp/infrastructure/adapters/feed_parser.py, tests/unit/infrastructure/test_feed_parser.py, tests/fixtures/discovery/feeds/</files>
<action>Parse RSS 2.0 and Atom from bytes/text using stdlib XML unless a dependency is demonstrably required. Extract stable entry ID, title, link, published/updated, author and summary. Disable external entity resolution. Malformed entries are item-level errors.</action>
<verify>pytest -q tests/unit/infrastructure/test_feed_parser.py</verify>
<done>RSS/Atom/malformed/duplicate fixture tests pass offline.</done>
</task>

<task type="auto">
<name>05.2 Implement RSS source adapter</name>
<files>src/github_stars_contrib_mcp/infrastructure/adapters/rss_source.py, tests/unit/infrastructure/test_rss_source.py</files>
<action>Fetch registered feed URL through ContentFetcher only; parse entries; emit SourceItem IDs from feed GUID or canonical link. Cursor records most recent stable ID/time without losing same-timestamp entries. Map article/blog type hint but leave final normalizer authoritative.</action>
<verify>pytest -q tests/unit/infrastructure/test_rss_source.py</verify>
<done>Incremental resync produces no duplicate SourceItems and never bypasses safe fetch.</done>
</task>

<task type="auto">
<name>05.3 Discover feeds/structured article links on trusted personal sites</name>
<files>src/github_stars_contrib_mcp/infrastructure/adapters/website_source.py, tests/unit/infrastructure/test_website_source.py</files>
<action>For ownership explicit/verified website sources only, fetch the registered origin, detect `<link rel=alternate>` RSS/Atom and bounded schema.org/OpenGraph article metadata. Follow only same-origin/trusted discovered feed URLs. Do not crawl arbitrary internal links in v0.3.0.</action>
<verify>pytest -q tests/unit/infrastructure/test_website_source.py</verify>
<done>Website fixtures discover feeds/metadata while cross-origin/untrusted links remain evidence only.</done>
</task>

<task type="auto">
<name>05.4 Register adapters and contract fixtures</name>
<files>src/github_stars_contrib_mcp/di/discovery.py, tests/contract/test_rss_discovery_contract.py</files>
<action>Register RSS and website adapters. Contract test: source registry → orchestrator → candidate persistence using mocked safe fetch and real parser/SQLite temp DB.</action>
<verify>pytest -q tests/contract/test_rss_discovery_contract.py</verify>
<done>Offline end-to-end contract yields deterministic article/blog candidates with evidence/provenance.</done>
</task>
