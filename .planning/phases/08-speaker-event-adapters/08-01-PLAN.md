---
phase: 08
plan: 01
name: speaker-event-adapters
issue: 32
requirements: [SPEAK-01, SPEAK-02, SPEAK-03]
depends_on: [03,04]
autonomous: true
---
# Phase 08 Plan — Speaker and event discovery

<task type="auto">
<name>08.1 Define session/event source normalization</name>
<files>src/github_stars_contrib_mcp/application/discovery/session_normalizer.py, tests/unit/application/test_session_normalizer.py</files>
<action>Normalize session title, description, date, event name, speaker identity, session URL and recording/slides URLs. Candidate type hints TALK/WORKSHOP only when evidence supports them; event listing alone does not prove speaker ownership.</action>
<verify>pytest -q tests/unit/application/test_session_normalizer.py</verify>
<done>Fixtures distinguish speaker sessions from unrelated agenda items.</done>
</task>

<task type="auto">
<name>08.2 Implement Sessionize-style public adapter</name>
<files>src/github_stars_contrib_mcp/infrastructure/adapters/sessionize_source.py, tests/unit/infrastructure/test_sessionize_source.py</files>
<action>Implement against documented/public event/session JSON endpoints only. Source registry stores the event/public API URL explicitly. Match the user's speaker identity using verified name/profile identifiers and preserve session/event evidence. No guessing by fuzzy name alone.</action>
<verify>pytest -q tests/unit/infrastructure/test_sessionize_source.py</verify>
<done>Recorded fixtures yield only the verified speaker's sessions with canonical evidence.</done>
</task>

<task type="auto">
<name>08.3 Implement Pretalx-style public adapter</name>
<files>src/github_stars_contrib_mcp/infrastructure/adapters/pretalx_source.py, tests/unit/infrastructure/test_pretalx_source.py</files>
<action>Use public schedule/API data from an explicitly registered event. Map persons/sessions with stable IDs and URLs; preserve event/session timestamps and links. Missing public API becomes limited/unavailable capability, not scraping fallback.</action>
<verify>pytest -q tests/unit/infrastructure/test_pretalx_source.py</verify>
<done>Fixture tests cover public data, missing person match and schedule updates.</done>
</task>

<task type="auto">
<name>08.4 Add bounded generic event page extraction</name>
<files>src/github_stars_contrib_mcp/infrastructure/adapters/event_page_source.py, tests/unit/infrastructure/test_event_page_source.py</files>
<action>Only explicit/verified event URLs may be fetched. Extract JSON-LD/OpenGraph and visible metadata via untrusted-content boundary. Never recursively crawl. Candidate confidence stays low/medium unless speaker identity and session title are both evidenced.</action>
<verify>pytest -q tests/unit/infrastructure/test_event_page_source.py</verify>
<done>Injection/cross-origin fixtures remain evidence-only and cannot trigger further fetches.</done>
</task>
