---
phase: 11
plan: 01
name: review-publish-mcp
issue: JIT
requirements: [REVIEW-01, REVIEW-02, PUB-01, PUB-02, PUB-03]
depends_on: [10]
autonomous: true
---
# Phase 11 Plan — Review and publish MCP workflows

<task type="auto">
<name>11.1 Add candidate review application service</name>
<files>src/github_stars_contrib_mcp/application/use_cases/review_candidates.py, tests/unit/application/test_review_candidates.py</files>
<action>Implement list/filter candidate, get detail, approve, edit+approve, reject and defer. Edits validate against existing contribution model constraints; every decision stores timestamp/reason/changed fields. Exact duplicates cannot be approved without an explicit separate override policy, which v0.3.0 does not expose.</action>
<verify>pytest -q tests/unit/application/test_review_candidates.py</verify>
<done>Lifecycle/audit tests cover approve/reject/defer/edit and duplicate blockers.</done>
</task>

<task type="auto">
<name>11.2 Add publication service with final recheck</name>
<files>src/github_stars_contrib_mcp/application/use_cases/publish_candidates.py, tests/unit/application/test_publish_candidates.py</files>
<action>Accept approved candidate IDs; reload candidate, rerun current Stars duplicate check, validate required fields, derive stable client ID, call existing create/upsert Stars contribution path, then persist published result/provenance atomically. Support dry_run returning exact proposed payloads with no Stars write.</action>
<verify>pytest -q tests/unit/application/test_publish_candidates.py</verify>
<done>Unapproved/stale-duplicate candidates never call Stars client; successful publish stores result and becomes idempotent.</done>
</task>

<task type="auto">
<name>11.3 Expose source/discovery/review MCP tools</name>
<files>src/github_stars_contrib_mcp/tools/discovery_sources.py, src/github_stars_contrib_mcp/tools/discovery_candidates.py, tests/unit/tools/test_discovery_tools.py</files>
<action>Register tools: `bootstrap_sources`, `list_sources`, `add_source`, `sync_source`, `discover_contributions`, `list_candidates`, `get_candidate`, `review_candidate`. Tool handlers only validate input/call application services/format output.</action>
<verify>pytest -q tests/unit/tools/test_discovery_tools.py</verify>
<done>Tools expose evidence/confidence/capability data without business-rule duplication.</done>
</task>

<task type="auto">
<name>11.4 Expose publish tool and server wiring</name>
<files>src/github_stars_contrib_mcp/tools/publish_candidates.py, src/github_stars_contrib_mcp/server.py, src/github_stars_contrib_mcp/shared.py, tests/unit/test_server.py</files>
<action>Add `publish_approved_candidates(candidate_ids, dry_run=true)` with dry_run default true. Real publish requires candidates already approved; tool cannot set approval and publish in one call. Import/register modules in server bootstrap and preserve existing tools/transports.</action>
<verify>pytest -q tests/unit/test_server.py tests/unit/tools/test_discovery_tools.py</verify>
<done>MCP smoke tests show discover/review/publish tools registered and existing Stars tools unchanged.</done>
</task>
