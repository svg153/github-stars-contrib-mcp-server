---
phase: 06
plan: 01
name: github-adapter
issue: 22
requirements: [GH-01, GH-02, GH-03, GH-04]
depends_on: [04]
autonomous: true
---
# Phase 06 Plan — GitHub discovery adapter

<task type="auto">
<name>06.1 Define GitHub eligibility rules</name>
<files>src/github_stars_contrib_mcp/application/discovery/github_eligibility.py, tests/unit/application/test_github_eligibility.py</files>
<action>Encode conservative rules: routine commits/stars/forks are not candidates. Candidate-producing evidence may include owned project releases, authored substantial repositories/projects, authored documentation/blog-like GitHub Pages records, and explicitly configured notable PR/issue/community activity. Every accepted rule emits a reason code; default deny unknown activity.</action>
<verify>pytest -q tests/unit/application/test_github_eligibility.py</verify>
<done>Positive/negative fixtures demonstrate low-spam default-deny behavior.</done>
</task>

<task type="auto">
<name>06.2 Implement GitHub API source adapter</name>
<files>src/github_stars_contrib_mcp/infrastructure/adapters/github_source.py, tests/unit/infrastructure/test_github_source.py</files>
<action>Use httpx with optional dedicated GitHub token setting; never reuse a Stars token implicitly. Query supported REST endpoints for the trusted username/repositories/releases and configured contribution surfaces. Handle Link pagination, ETag where practical and rate-limit headers. Emit canonical github.com URLs and stable node/REST IDs.</action>
<verify>pytest -q tests/unit/infrastructure/test_github_source.py</verify>
<done>Mocked API tests cover pagination, rate limits, anonymous limited mode and stable IDs.</done>
</task>

<task type="auto">
<name>06.3 Add settings/capability reporting</name>
<files>src/github_stars_contrib_mcp/config/settings.py, env.template, tests/unit/test_settings.py</files>
<action>Add optional `GITHUB_DISCOVERY_TOKEN`. Capability is available with token, limited without token, unavailable only for explicit auth failures. Never log token values.</action>
<verify>pytest -q tests/unit/test_settings.py</verify>
<done>Settings/capability behavior is explicit and secret-safe.</done>
</task>

<task type="auto">
<name>06.4 Register and contract-test GitHub discovery</name>
<files>src/github_stars_contrib_mcp/di/discovery.py, tests/contract/test_github_discovery_contract.py</files>
<action>Register adapter and run mocked API → eligibility → orchestrator → SQLite. Assert evidence URLs, reason codes, cursor behavior and second-run idempotency.</action>
<verify>pytest -q tests/contract/test_github_discovery_contract.py</verify>
<done>Contract yields explainable candidates and zero duplicates on replay.</done>
</task>
