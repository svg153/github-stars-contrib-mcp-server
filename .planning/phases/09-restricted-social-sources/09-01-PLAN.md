---
phase: 09
plan: 01
name: restricted-social-sources
issue: JIT
requirements: [SOCIAL-01, SOCIAL-02, SOCIAL-03]
depends_on: [03,04]
autonomous: true
---
# Phase 09 Plan — Restricted social sources

<task type="auto">
<name>09.1 Model restricted-provider capabilities</name>
<files>src/github_stars_contrib_mcp/application/discovery/social_capabilities.py, tests/unit/application/test_social_capabilities.py</files>
<action>Define X and LinkedIn capability modes: explicit_url, export_import, official_api, unavailable. No `scrape` mode. Capability output states required credential/export/user action and why automation is limited.</action>
<verify>pytest -q tests/unit/application/test_social_capabilities.py</verify>
<done>Unsupported access cannot silently switch to browser/scraper behavior.</done>
</task>

<task type="auto">
<name>09.2 Add explicit social URL ingestion</name>
<files>src/github_stars_contrib_mcp/infrastructure/adapters/social_url_source.py, tests/unit/infrastructure/test_social_url_source.py</files>
<action>Accept user-registered post URLs as evidence seeds. Fetch only when safe-fetch/provider policy allows public metadata; otherwise persist URL/title/date supplied by user as low-confidence evidence requiring review. Never require authenticated session cookies.</action>
<verify>pytest -q tests/unit/infrastructure/test_social_url_source.py</verify>
<done>Explicit URLs enter the common pipeline without bypass logic or fabricated metadata.</done>
</task>

<task type="auto">
<name>09.3 Add export/import adapter contract</name>
<files>src/github_stars_contrib_mcp/infrastructure/adapters/social_export_source.py, tests/unit/infrastructure/test_social_export_source.py</files>
<action>Parse a versioned neutral JSON/CSV import schema plus provider-specific mappers only when fixtures/documentation are known. Require local user-provided files; never upload exports. Emit stable provider post IDs/URLs when present and mark imported evidence origin.</action>
<verify>pytest -q tests/unit/infrastructure/test_social_export_source.py</verify>
<done>Import is deterministic, local-only and reusable for X/LinkedIn exports without coupling orchestration to provider format.</done>
</task>

<task type="auto">
<name>09.4 Document official API extension point</name>
<files>src/github_stars_contrib_mcp/domain/ports/source_adapter.py, docs/sources/restricted-social.md, tests/unit/domain/test_source_adapter_contract.py</files>
<action>Document how a compliant OAuth/API adapter plugs into SourceAdapter and how secrets/scopes are isolated. Do not ship brittle provider API code without a supported access contract.</action>
<verify>pytest -q tests/unit/domain/test_source_adapter_contract.py</verify>
<done>Core supports future compliant connectors while v0.3.0 remains useful via URL/export modes.</done>
</task>
