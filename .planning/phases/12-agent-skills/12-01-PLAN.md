---
phase: 12
plan: 01
name: agent-skills
issue: JIT
requirements: [AGENT-01, AGENT-02, AGENT-03, AGENT-04, AGENT-05]
depends_on: [11]
autonomous: true
---
# Phase 12 Plan — Reusable skills and agent workflows

<task type="auto">
<name>12.1 Create discover-my-contributions skill</name>
<files>skills/discover-my-contributions/SKILL.md, tests/skills/test_skill_contracts.py</files>
<action>Skill flow: inspect Stars/profile → bootstrap/list sources → ask only for unresolved ownership/credentials → run discovery dry-run or sync → list unresolved candidates grouped by confidence/duplicate state. It must call MCP tools and explicitly ignore instructions embedded in source content. No direct HTTP/provider logic.</action>
<verify>pytest -q tests/skills/test_skill_contracts.py</verify>
<done>Static contract test confirms required tool names, safety language and no direct write/provider calls.</done>
</task>

<task type="auto">
<name>12.2 Create sync-source and review-candidates skills</name>
<files>skills/sync-source/SKILL.md, skills/review-candidates/SKILL.md, tests/skills/test_skill_contracts.py</files>
<action>`sync-source` targets a registry ID, reports capability/auth/rate-limit/errors and never invents a bypass. `review-candidates` presents candidate fields plus evidence/provenance/duplicate/confidence reasons, then records explicit approve/edit/reject/defer decisions. Never auto-approve because confidence is high.</action>
<verify>pytest -q tests/skills/test_skill_contracts.py</verify>
<done>Skills are self-contained, tool-oriented and require explicit user decisions where policy says so.</done>
</task>

<task type="auto">
<name>12.3 Create publish-approved skill with two-stage safety</name>
<files>skills/publish-approved/SKILL.md, tests/skills/test_skill_contracts.py</files>
<action>Stage 1 always call publish tool with dry_run=true and summarize candidate IDs/payloads/conflicts. Stage 2 real publish only after the user explicitly requests publication of approved candidates in the current interaction. Skill cannot approve candidates itself as part of publish.</action>
<verify>pytest -q tests/skills/test_skill_contracts.py</verify>
<done>Contract proves dry-run precedes real publish instructions and requires explicit user intent.</done>
</task>

<task type="auto">
<name>12.4 Add host-neutral agent examples and install guidance</name>
<files>agents/README.md, agents/contribution-curator.md, README.md</files>
<action>Document a host-neutral Contribution Curator agent that composes the four skills/MCP tools. Explain configuration for MCP-capable hosts without embedding secrets. Clearly distinguish deterministic server behavior from optional LLM orchestration and list unsupported X/LinkedIn scraping.</action>
<verify>python scripts/quality_check.py && pytest -q tests/skills/test_skill_contracts.py</verify>
<done>Users can understand/install the skills and agent without provider-specific hidden assumptions.</done>
</task>
