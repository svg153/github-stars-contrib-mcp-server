---
phase: 13
plan: 01
name: quality-release
issue: JIT
requirements: [QUAL-01, QUAL-02, OBS-01, DOC-01, REL-01]
depends_on: [12]
autonomous: true
---
# Phase 13 Plan — Evaluation, observability, docs and release

<task type="auto">
<name>13.1 Build labeled discovery evaluation corpus</name>
<files>tests/evals/fixtures/discovery_cases.json, tests/evals/test_discovery_eval.py, docs/evals/discovery-quality.md</files>
<action>Create synthetic/public-safe cases spanning RSS, GitHub, YouTube, speaker events, explicit social URLs, exact duplicates, likely duplicates, unrelated noise and prompt injection. Labels: eligible type, expected duplicate class, minimum/maximum confidence band and required evidence. No personal/private data.</action>
<verify>pytest -q tests/evals/test_discovery_eval.py</verify>
<done>Eval computes precision-oriented eligibility/dedupe assertions and fails on regressions rather than printing vanity metrics only.</done>
</task>

<task type="auto">
<name>13.2 Add privacy-safe discovery telemetry</name>
<files>src/github_stars_contrib_mcp/observability/discovery.py, tests/unit/observability/test_discovery_observability.py</files>
<action>Emit structured metrics/log fields: run_id, source_type, capability, item/candidate counts, duplicate class, duration and error class. Exclude URLs when unnecessary, titles/descriptions/page bodies/prompts/tokens always. Reuse repository observability conventions.</action>
<verify>pytest -q tests/unit/observability/test_discovery_observability.py</verify>
<done>Tests assert useful dimensions and absence of sensitive/content fields.</done>
</task>

<task type="auto">
<name>13.3 Complete docs and capability matrix</name>
<files>README.md, docs/sources/README.md, docs/security/discovery-threat-model.md, CHANGELOG.md</files>
<action>Document user journey, MCP tools, four skills, source matrix (available/limited/credential required/unsupported), SQLite location, privacy model, prompt-injection/SSRF controls, X/LinkedIn limitations and migration notes. Do not claim integration evidence that was skipped.</action>
<verify>python scripts/quality_check.py && python -m compileall -q src</verify>
<done>Docs truthfully describe supported behavior, setup and non-goals.</done>
</task>

<task type="auto">
<name>13.4 Run release gates and produce v0.3.0 verification</name>
<files>.planning/phases/13-quality-release/VERIFICATION.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
<action>Run `make test`, focused evals, compile/type/lint commands available in Makefile, MCP registration smoke test and an end-to-end offline fixture flow through source→candidate→review→publish dry-run. If a Stars token is explicitly available, run credentialed integration separately; otherwise record unavailable. Update requirement checkboxes only from evidence.</action>
<verify>make test</verify>
<done>Verification contains exact commands/results, unresolved risks and release recommendation; planning state is reconciled.</done>
</task>
